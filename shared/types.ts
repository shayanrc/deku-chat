// Transitional shim: the real types now live in @deku/core (P1 of the monorepo
// split). This file keeps legacy `../shared/types` imports working until the
// legacy src/ and server/ trees are deleted in P6.
export * from '@deku/core';
